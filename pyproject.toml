[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "currency-conversion-api-automation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests==2.34.2",
    "pydantic==2.13.4",
    "pytest==9.1.1",
    "pytest-html==4.2.0",
]